# Making reusable composite actions documented at
# https://docs.github.com/en/actions/tutorials/create-actions/create-a-composite-action#creating-a-composite-action-within-the-same-repository
name: 'Receive a large zone via multi-part AXFR from the NSD primary.'
description: 'Receive a large zone via multi-part AXFR from the NSD primary.'
defaults:
  # see: https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#defaultsrunshell
  run:
    shell: bash --noprofile --norc -eo pipefail -x {0}
inputs:
  log-level:
    description: The level of logging that Cascade should output.
    required: false
    default: debug
    type: choice
    options:
      - error
      - warning
      - info
      - debug
      - trace
runs:
  using: "composite"
  steps:
    - uses: ./.github/actions/prepare-systest-env

    - name: Replace the tiny NSD primary example zone with a much larger one
      run: |
        ZONE_DIR=$(integration-tests/scripts/get-default-path.sh test-env:nameserver-base-dir)
        ZONE_FILE="${ZONE_DIR}/nsd-primary/zones/example.test.primary-zone"
        INTEGRATION_TEST_DIR="${PWD}/integration-tests/multipart-axfr"
        cp "${INTEGRATION_TEST_DIR}"/example.test.zone "${ZONE_FILE}"
        ./integration-tests/scripts/manage-test-environment.sh control nsd-primary reload example.test

    - name: Query changed SOA at the NSD primary
      run: |
        timeout=10 # seconds
        start=$(date +%s)
        until dig +short @127.0.0.1 -p 1055 example.test SOA | grep -q 'ns1.example.test. mail.example.test. 2 60 60 3600 5'; do
          if (($(date +%s) > (start + timeout))); then
            echo "::error:: timeout: NSD did not reload the modified zonefile"
            exit 1
          fi
          sleep 1
        done

    - uses: ./.github/actions/setup-and-start-cascade
      with:
        log-level: ${{ inputs.log-level }}

    - name: Add the NSD primary zone to Cascade
      run: |
        cascade zone add --policy default --source 127.0.0.1:1055 example.test

    - name: Check zone status
      run: |
        timeout=10 # seconds
        start=$(date +%s)
        until cascade zone status example.test | grep -q "Published zone available"; do
          if (($(date +%s) > (start + timeout))); then
            cascade zone status example.test
            echo "::error:: timeout: zone status did not report published zone available"
            exit 1
          fi
          sleep 1
        done

    # TODO: Verify that Cascade actually handled more than one AXFR part.
    # Currently there is no NSD or Cascade metric that can be used to check
    # this.

    - name: Query the zone at the unsigned review server
      run: |
        dig +noall +onesoa +answer @127.0.0.1 -p 4540 example.test AXFR &> example.test.zone.log

        # Check that the received zone matches the included ZONEMD RR.
        # FYI the ZONEMD RR was generated using ldns-signzone -z sha512 -Z example.test.zone
        if ! ldns-verify-zone -Z example.test.zone.log 2>&1 | tee ldns-verify-unsigned-review-zone.log; then
          echo "::error:: the unsigned review zone failed the ldns-verify-zone check"
          exit 1
        fi

        NUM_RRS=$(cat example.test.zone.log | wc -l)
        if [[ ${NUM_RRS} -ne 506 ]]; then
          echo "::error:: the unsigned review zone has the wrong number of records: ${NUM_RRS} != 506"
          exit 1
        fi

    - name: Query the signed zone at the publication server
      run: |
        dig +noall +onesoa +answer @127.0.0.1 -p 4542 example.test AXFR &> example.test.zone.log

        # Check that the zone is correctly signed.
        # Note: Cascade doesn't support adding the ZONEMD RR to the signed zone so we don't check
        # for that and don't expect it in the output.
        if ! ldns-verify-zone example.test.zone.log 2>&1 | tee ldns-verify-published-zone.log; then
          echo "::error:: the unsigned review zone failed the ldns-verify-zone check"
          exit 1
        fi

        NUM_RRS=$(cat example.test.zone.log | wc -l)
        if [[ ${NUM_RRS} -ne 2016 ]]; then
          echo "::error:: the unsigned review zone has the wrong number of records: ${NUM_RRS} != 2016"
          exit 1
        fi

    - name: Print log files on any failure in this job
      uses: ./.github/actions/print-logfiles
      if: failure()
